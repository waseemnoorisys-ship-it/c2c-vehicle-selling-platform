// const Stripe = require("stripe");

// if (!process.env.STRIPE_SECRET_KEY) {
//   throw new Error("STRIPE_SECRET_KEY is missing in .env");
// }

// const stripe = new Stripe(process.env.STRIPE_SECRET_KEY, {
//     //what is meaning of .acacia?
//     //the meaning of .acacia is that it is the latest version of the stripe api that is available.
//   apiVersion: "2024-11-20.acacia", // optional; Stripe uses account default if omitted
// });

// module.exports = stripe;